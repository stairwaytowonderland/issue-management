name: Create Labels

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: 'Dry run'
        type: boolean
        default: true
      update-existing:
        description: 'Whether to update existing labels (e.g. rename, change color/description).'
        type: boolean

  workflow_run:
    workflows: ['_']
    types: [completed]

jobs:
  create-labels:
    name: Create Labels
    # if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    uses: stairwaytowonderland/label-management/.github/workflows/create-labels.yaml@v1
    secrets: inherit
    with:
      dry-run: ${{ github.event.workflow_run.conclusion }}
      update-existing: ${{ inputs.update-existing == true }}
